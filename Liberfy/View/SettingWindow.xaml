<Window
	x:Class="Liberfy.SettingWindow"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	xmlns:local="clr-namespace:Liberfy"
	xmlns:sys="clr-namespace:System;assembly=mscorlib"
	mc:Ignorable="d"
	ResizeMode="NoResize"
	Title="設定" Height="389" Width="560">
	<Window.Resources>
		<local:SettingWindowViewModel x:Key="ViewModel" />
		<sys:Double x:Key="tabItemWidth">140</sys:Double>

		<SolidColorBrush x:Key="TabPanelBackground" Color="#FF00355B" />

		<!--#region TabControl Template-->
		<Style x:Key="tabItemStyle" TargetType="{x:Type TabItem}">
			<Setter Property="Padding" Value="14 8" />
			<Setter Property="Background" Value="{x:Static Brushes.Transparent}" />
			<Setter Property="Foreground" Value="{x:Static Brushes.White}" />
			<Setter Property="Template">
				<Setter.Value>
					<ControlTemplate TargetType="{x:Type TabItem}">
						<Border
							x:Name="border"
							Padding="{TemplateBinding Padding}"
							Background="{TemplateBinding Background}">
							<ContentPresenter ContentSource="Header" />
						</Border>

						<ControlTemplate.Triggers>
							<Trigger Property="IsMouseOver" Value="True">
								<Setter TargetName="border" Property="Background" Value="#2fff" />
							</Trigger>

							<Trigger Property="IsSelected" Value="True">
								<Setter TargetName="border" Property="Background" Value="#dfff" />
								<Setter Property="Foreground" Value="{DynamicResource TabPanelBackground}" />
							</Trigger>
						</ControlTemplate.Triggers>
					</ControlTemplate>
				</Setter.Value>
			</Setter>
		</Style>

		<Style x:Key="tabControlStyle" TargetType="{x:Type TabControl}">
			<Setter Property="TabStripPlacement" Value="Left" />
			<Setter Property="Background" Value="#fafafa" />
			<Setter Property="Template">
				<Setter.Value>
					<ControlTemplate TargetType="{x:Type TabControl}">
						<Grid>
							<Grid.ColumnDefinitions>
								<ColumnDefinition Width="auto" />
								<ColumnDefinition />
							</Grid.ColumnDefinitions>

							<TabPanel
								x:Name="HeaderPanel"
								IsItemsHost="True"
								KeyboardNavigation.TabIndex="1"
								Width="{StaticResource tabItemWidth}"
								Background="{DynamicResource TabPanelBackground}" />
							
							<Border 
								Grid.Column="1"
								Background="{TemplateBinding Background}">
								<ContentPresenter />
							</Border>
						</Grid>
					</ControlTemplate>
				</Setter.Value>
			</Setter>
			<Setter Property="ItemContainerStyle" Value="{StaticResource tabItemStyle}" />
		</Style>
		<!--#endregion-->
	</Window.Resources>
	<Window.DataContext>
		<StaticResource ResourceKey="ViewModel" />
	</Window.DataContext>
	<DockPanel>
		<Grid
			DockPanel.Dock="Top" Height="32"
			Background="{DynamicResource TabPanelBackground}">
			<Grid.ColumnDefinitions>
				<ColumnDefinition Width="140" />
				<ColumnDefinition />
				<ColumnDefinition Width="auto" />
			</Grid.ColumnDefinitions>

			<Border Grid.Column="1" BorderThickness="1 0 0 0" Grid.ColumnSpan="2" BorderBrush="#1000" Background="#6000" />

			<Border Background="#6000">
				<TextBlock
					Text="設定"
					FontWeight="Bold"
					VerticalAlignment="Center"
					HorizontalAlignment="Center"
					Foreground="{x:Static Brushes.White}" />
			</Border>

			<TextBlock
				Grid.Column="1"
				FontWeight="Bold"
				VerticalAlignment="Center"
				HorizontalAlignment="Center"
				Foreground="{x:Static Brushes.White}"
				Text="{Binding ElementName=tabControl, Path=SelectedItem.Header}" />
		</Grid>
		
		<TabControl x:Name="tabControl" Style="{StaticResource tabControlStyle}">
			<TabItem Header="全般">
				<Button />
			</TabItem>

			<TabItem Header="アカウント">

			</TabItem>

			<TabItem Header="通知">

			</TabItem>
		</TabControl>
	</DockPanel>
</Window>
